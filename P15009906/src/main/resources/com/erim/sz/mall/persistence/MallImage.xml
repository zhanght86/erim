<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mallimage">
	
	<!-- 查询 -->
	<select id="selectAll" resultType="mallImageBean">
		SELECT
			id 			   AS id,
			mie_imageurl   AS mieImageurl,
			mie_createtime AS mieCreatetime,
			mie_createuser AS mieCreateuser
		FROM
			mall_image
	</select>
</mapper>